import React from 'react'

export const Products = () => {
    // Завдання:
    // 1. Створити тип під дані з https://fakestoreapi.com/products [GET]
    // 2. Зробити запит на axios використовуючи useEffect + useState (Posts.tsx) 1 рендер комопненту
    // 3. Додати isError та isLoading state для помилок та індикації прогрузки
    // 4. Вивести продукти у вигляді карток (map) + .css
    // (Вивести комопнент Products у App.tsx (Підключити))

  return (
    <div>Products</div>
  )
}
