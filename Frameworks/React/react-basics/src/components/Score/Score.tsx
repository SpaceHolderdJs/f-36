import { useState } from "react"

export const Score = () => {
    const [score1, setScore1] = useState<number>(0);
    const [score2, setScore2] = useState<number>(0);
    const [score3, setScore3] = useState<number>(0);

    // Завдання
    // 1. Вивести 3 оцінки на екран (score1,2,3)
    // 2. Під кожною оцінкою вивести кнопку, що додає до оцінки +5
    // 3. Оцінка, яку збільшили має автоматично перемалюватися (setScore1,2,3)
    // 4. СSS за бажанням

    return <div>
        
        
        {/* .... */}
    
    </div>
}