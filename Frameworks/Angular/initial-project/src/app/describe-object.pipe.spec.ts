import { DescribeObjectPipe } from './describe-object.pipe';

describe('DescribeObjectPipe', () => {
  it('create an instance', () => {
    const pipe = new DescribeObjectPipe();
    expect(pipe).toBeTruthy();
  });
});
